# Picxel 规格 v0.5

## 产物与格式

静态 `tile`、`item`、`sprite`，尺寸固定 32 / 64 / 128，单张最多 16 色；透明度 0 或 255。导出原尺寸 PNG、最近邻 4× 预览、精灵表 PNG + JSON、自包含 HTML。动画、非方形画布和编辑器不在当前范围。

`.pxg` 为 UTF-8 文本。头部 `键: 值`，一行 `---` 后恰好 `size` 行，每行 `size` 字符。例如：

```text
name: potion-red
size: 32
kind: item
palette: custom
A: #222034
B: #ac3232
C: #ffffff
---
................................
...（省略：实际文件须有完整的 32 行）
```

色号 `A`–`P`，`.` 透明；使用的色号必须声明。`palette` 为 `db32`、`custom` 或 `.pal` 路径。`.pal` 每行一个小写 `#rrggbb`，图纸可相对自身目录引用；颜色必须属于指定色板。相关素材可共享色板。

`item` / `sprite` 四角透明且主体非空；导入高分辨率素材保留至少一目标像素边距，原生尺寸导入保留已有布局。`tile` 接缝色差、透明像素和 8 邻域孤点发出警告。格式校验不代表美术验收。

## 制作

简单素材：助手在目标网格放置形状、色块、细节，渲染后修正。

复杂参考：原图 → 锚定单 → 多图风格鉴定与必要的用户选择 → 区域马赛克 → 助手效果图（可跳过）→ 取色 → 色板归并后按格投票 → 低对比孤点清理 → 看图修画 → 锚定单验收 → 打包。单图跳过整批风格检查。

马赛克仅简化区域颜色，保留原透明轮廓，区域使用全画布对齐网格，`fine` 恢复源像素。它不能理解并删除语义上的纹理或装饰。去背景仅处理边界连通的近似单色，已有 alpha 优先。复杂背景需要效果图或遮罩。

取色完整统计可见源像素，按透明覆盖、边缘可信度及区域预算加权，在 Oklab 视觉色差空间分组并分配色位。自动颜色来自真实源 RGB，不混出不存在的平均色；颜色本来能放进预算的图保留原色。显式锁色优先，再保留有足够支持的亮暗端点、精细区域代表色，剩余色位优先覆盖仍有较大加权色差的颜色。近透明边界及不足四分之一目标像素加权面积的零散颜色降低优先级。锚定单 `palette` 可预留十六进制关键色，重复值只占一个色位；最多 16 色，完整 16 色列表可锁定批次色板。独立取色与 `import --palette custom` 共用此算法。导入先归到色板再投票，透明占比独立计算，达到半格则透明，不使用抖动。

`smooth` 只合并无同色 8 邻居、且与多数邻色 RGB 距离不超过 48 的孤点；`keep` 色号受保护。高对比像素可能是眼睛或高光，交给视觉验收。`derive` 同样独立判断透明占比，小尺寸关键细节可能需要重画；`--keep` 仅保护后续清理。

## 效果图交接

Python 不调用对话工具，也不启动 Codex / Claude 子进程。

- `none` 使用本地马赛克。
- `codex` / `claude` / `api` 是来源标签，读取 `concept --result IMAGE` 或 `batch --concept-dir DIR` 的图片。
- `batch` 在风格检查及该项选择完成后写 `<name>.prompt.txt`。一般读取 `<name>.png`；批准统一风格的异类读取 `<name>.unified.png`。非 `none` 缺图时报告 `needs-concept`；即使 `none`，批准统一的项目也需要新的统一风格效果图。
- 效果图须检查真实 alpha、背景、姿势、锚定特征。默认 `auto` 剥离边界连通背景。明确选择主体不含的底色时，使用 `concept --background 'key:#ff00ff'` 或 `batch --concept-background 'key:#ff00ff'`，连发丝或把手内的封闭底色一起去掉。棋盘格不能冒充透明。
- Picxel 不需要 API key；宿主工具可用性和用量限制由宿主决定。

## 人类面板

`panel` 起一个只依赖标准库的本地页面（http.server + tkinter 对话框）。人在页面上选图片（系统文件对话框，单张一张 / 批量多选，导入目录即所选文件所在目录）、导出位置、单张或批量（≤20）、尺寸、画风鉴定与子智能体并行两个勾选；页面写 `~/.picxel/current-job.json` 和导出目录里的 `picxel.status.json`（waiting），不调用助手。助手用 `job show / start / done / stop --note` 读任务、标状态；页面轮询状态文件和导出目录里的 `<name>-<size>@4x.png`：running 时统一转圈计时（长时间无新文件提示可能中断，可手动结束等待），done / interrupted 时按素材一排排显示原图与各尺寸成品（原生 PNG 按 2 倍真实比例显示），缺的标"未生成"并附原因（来自导出目录的 `batch-report.json`：`needs-*` / `check-failed` / `failed` / 尚无条目），同时露出报告里的校验警告与 `needs-face-review`；给出用时（`started`→`finished`）、结束时刻和打开导出位置的按钮。点卡片进对比层（原图 + 各尺寸，1/2/4/8 倍）。`style_check` 为真时页面读导入目录的 `batch.style.json`，把 `match:false` 的素材列为问题，用户点选后写回 `choice`（只接受 original / unify，只对离群素材）；`job show` 也会打印这些选择。`/api/clear` 删除任务文件（新任务）；导出目录不存在时页面直接说明。

## 批量与验收

已有外部效果图时直接进入效果图处理与取色，不重建原图马赛克。准备阶段及 `none` 本地路线继续生成所需马赛克。降采样使用每格颜色直方图计票，平票仍按原图行优先的首次出现顺序决胜，透明覆盖规则不变。

`batch --only NAME ...` 只处理本次选中的素材，风格判断仍来自完整输入批次；其他已有输出不变。`batch-report.json` 描述本次调用，子集运行带 `selected` 列表，不应被解释成整批都已完成。控制台提供提示文件、待保存文件名、风格参照和必要的面部复核入口；详细报告继续保留。

`show` 默认只打印元数据与色板，`--box` 为明确的局部范围，`--full` 才打印全部像素行。只生成请求的尺寸；查看原图、风格选择、原始视线判断与最终视觉验收不因省 token 而跳过。

### 可选面部修画

锚定单 `faces` 仅标注可见人物/动物面部，包括源图归一化 `box`、`complex` 布尔值、`expression` 与 `reason`，复杂面部还需从高清原图记录 `gaze`。助手负责看图判断，Python 不用暗色块推断眼睛。普通物品不附加处理；清晰面部保留普通流程。复杂面部在像素化后逐尺寸复核，若眼睛和嘴巴已清楚则保留，否则仅在眼口小区域修画；先保留原图眼仁位置、眼白露出侧、眼睑开合与视线方向，再增加辨识度。眉毛、鼻子、刘海和面部其他区域保持原样。

批量报告的 `face_review` 按图纸记录 `preserve` 或 `needs-face-review`，复杂面部附带 `.face-prompt.txt`。该状态附属于已生成底稿的视觉验收，不代表自动重绘或语义验收通过。单图可用 `face --prompt-only` 获取同样的指导。

`face --patch` 读取指定源图纸名称、尺寸、`feature: eye|mouth` 和局部矩形字符行；矩形坐标由助手在最终网格重新确定。只允许已标注的复杂面部、既有颜色或空闲色号声明的合规颜色，保留矩形外像素和透明轮廓。眉毛、鼻子和整脸修画类型被拒绝；开放眼补丁只能修改所声明眼区内的像素。可选 `eyes` 检查睁眼区是否同时有明暗像素且 Oklab 明度差至少 0.3；闭眼、侧脸和有意遮挡不强制套睁眼模板。修画稿另存 `-face.pxg` 及 PNG，保留源文件。细则见 [references/faces.md](references/faces.md)。

### 批次行为

画风鉴定默认关闭，用户勾选（CLI `--style-check`）才启用。启用且至少两张参考图时，助手先按线条、造型语言、明暗、材质和色彩处理进行视觉鉴定，Python 不按像素颜色统计判画风。检查单 `batch.style.json` 位于输入目录，保存共用风格描述、基准素材顺序、逐项匹配结论/原因以及用户选择。输入文件名和 SHA256 用于防止把旧判断、旧选择误套到已更换的图片；输入变化时暂停复查并保留旧检查单。

匹配素材自动继续，明显异类在 `needs-style-choice` 等待 `original`（保留原图）或 `unify`（统一画风），报告给出 `question` 与 `choices`。保留原图使用原有单图提示词；统一只调整绘画表现，保留身份、姿势、轮廓、关键特征和标志性颜色。同批匹配素材与统一素材共享风格描述和已确认的效果图参照。无明确多数时由用户选择基准，不自行指定。

生成顺序从第一个基准素材开始，再到后续基准与其余素材。报告的 `style_references` 是应供给生图工具的已确认素材 ID，`concept_file` 是预期结果文件名。模型看图与用户选择由当前助手执行，未来面板可消费这些字段；当前不包含面板开发。检查单格式与操作说明见 [references/batch-style.md](references/batch-style.md)。

每张 `<name>.png/.jpg/.jpeg/.webp` 配 `<name>.anchor.json`。输出包含中间 PNG、提示词、`.pal`、`.pxg`、渲染结果与 `batch-report.json`。独立批次使用独立输出目录。

报告：`needs-style-review` 等待助手完成风格鉴定；`needs-style-choice` 等待用户决定异常项的风格；`needs-concept` 等待效果图；`base-ready` 底稿格式合格；`check-failed` 校验失败；`failed` 处理失败。失败项目不拖垮整批。退出码 0 全部底稿就绪，1 存在失败，2 等待鉴定、选择或效果图。

自动底稿的 `review` 为 `pending`。助手在原生尺寸及放大预览检查轮廓、色块、细节预算、透明边界，按尺寸逐项回答锚定特征是否通过；视觉记录随交付给出，脚本不伪造语义验收。对比时明确标注参考、效果图、自动底稿与修画结果。
