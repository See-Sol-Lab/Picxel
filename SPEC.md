# pixelgrid 规格草案（v0.1，2026-09-12）

> 名字 `pixelgrid` 是占位，正式名待定。

## 一句话

让 AI 编程助手（Claude Code / Codex）按固定规格**逐格填色**批量产出像素游戏素材。AI 写图纸，脚本渲染和校验，开发者收货。不用生图模型猜像素，不离开编程助手，不另起服务。

## 解决谁的什么问题

独立像素游戏开发者没时间一张一张手搓地块、物品、角色。他们写一张清单，AI 按清单填，人只负责挑和改最后一成。

## 只做什么

- 静态素材：地块（tile）、物品（item）、角色静态姿势（sprite）。
- 三个尺寸档位，**其他一律不支持**：16×16、32×32、64×64。
- 一张图纸最多 16 色，颜色从公共主色板（DB32）里选；导入外部图时允许自定义色板。
- 导出：原尺寸 PNG、4 倍预览 PNG、整目录精灵表 PNG + JSON、一个自包含的 HTML 总览页。
- 导入：把现成 PNG 读成图纸（自动按格子降采样、颜色吸附到主色板），AI 接着改。

## 不做什么

- 不让素材动起来（帧对齐、锚点、动画状态机是引擎里的事）。
- 不接生图 API 出成品；生图最多用来出"效果图"给 AI 当参考，那一步在这个工具之外。
- 不做编辑器界面；总览页只看不改。
- 不做 32×48 这类非方形尺寸（第一版）。

## 图纸格式（AI 写的东西）

纯文本，扩展名 `.pxg`。头部是 `键: 值`，一行 `---`，然后恰好 H 行、每行恰好 W 个字符：

```
name: grass-01
size: 32
kind: tile
palette: db32
A: #4b692f
B: #6abe30
C: #99e550
---
AAAAAAAABAAAAAAAAAAAAAAAAAAABAAA
...（共 32 行）
```

- `size`：16 / 32 / 64。
- `kind`：`tile`（四边要能拼接）/ `item` / `sprite`（背景必须透明）。
- `palette`：`db32` 或 `custom`。`db32` 时每个色值必须在主色板里。
- 色号 `A`–`P`（最多 16 个）；`.` 是透明。
- 行里只能出现头部声明过的色号和 `.`。

## 流程（写进 SKILL.md 的规矩）

1. 看参考（用户给的图、描述、或效果图），定尺寸和 `kind`。
2. 先填**轮廓**（只用一个色和 `.`），渲染，看。
3. 再铺**大色块**（≤ 4 色），渲染，看。
4. 再加**明暗**（每个大色块各一深一浅），渲染，看。
5. 最后**描边**（最深色勾轮廓、清理孤立像素），渲染，`check` 过了才交。
6. 每次"看"都对着 `references/checklist.md` 挑毛病，改了再渲；至少两轮。

## 校验（脚本硬规矩）

- 尺寸在三档之内；行数、每行字符数精确。
- 色号 ≤ 16；`db32` 模式下色值 ∈ 主色板。
- `sprite` / `item`：四角必须透明。
- `tile`：左右两列、上下两行的平均色差低于阈值（接缝提示，warning）。
- 孤立像素（四邻全是别的色）计数，超过阈值 warning。
- 不合规不出 PNG。

## 批量

一个目录 = 一批。`sheet` 命令扫目录里全部 `.pxg`：全部 `check` → 逐张 `render` → 精灵表 + JSON + `index.html`。开发者开总览页，标出要重画的名字，AI 只重画那几张。

## 试验标准（决定要不要往下做）

用现在的模型，对着规矩，一次性填出：32×32 草地（四边能拼）、32×32 一个小人（透明背景、能看出是人、正面）、16×16 一个道具。三张里有两张不用人动手就能用，就算过。

## v0.3 追加：参考图流水线（2026-09-12 早，莉莉丝定）

原图 → **锚定单**（模型看图写：keep / drop / 区域细节预算）→ **区域马赛克**（去背景 + 按预算打块）→ **效果图**（provider 可插拔：`none` 直接用马赛克图；`codex` / `claude` / `api` 预留，codex 那条由 Sol 实现）→ **取色**（从效果图取 N 色）→ **降采样成底稿** → **擦 + 画 ≤ 20 步**（底稿当画布，橡皮擦先擦再补）→ **按锚定单验收** → 精灵表。

批量两种模式：排队（省 token）/ 并发子智能体（快、贵）；批量不改图，返工进单独模式。默认路径不用任何付费 API（国内用户没有）。

`batch` 命令把确定性的那半条流水线一口气跑完：一个目录 = 一批，每张参考图配一个 `<名字>.anchor.json`，马赛克 → 效果图 → 取色 → 底稿 → 除斑 → 校验 → 渲染，产出 `batch-report.json`；单张失败不拖垂整批。模型侧只剩擦画和验收，排队/并发只管这一段。
